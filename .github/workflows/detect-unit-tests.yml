name: Detect and Run Unit Tests with Coverage

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  check-spec-files:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false

    permissions:
      pull-requests: write
      contents: read

    steps:
      - name: Checkout PR branch code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Detect .spec.js Files in PR
        id: detect_files
        run: |
          BASE="${{ github.event.pull_request.base.ref }}"
          git fetch origin "$BASE"

          FILES=$(git diff --name-only origin/"$BASE"...HEAD | grep '\.spec\.js$' || true)
          echo "found_files=$FILES" >> "$GITHUB_OUTPUT"

      - name: Run Jest with Coverage
        run: |
          echo "🧪 Running tests with coverage..."
          npm run coverage

      - name: Upload HTML Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: coverage/lcov-report
          retention-days: 7

      - name: Post Jest Coverage Summary as PR Comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = 'coverage/coverage-summary.json';
            if (!fs.existsSync(path)) {
              core.setFailed('❌ coverage-summary.json not found');
            } else {
              const summary = JSON.parse(fs.readFileSync(path, 'utf8')).total;
              const format = (obj) => `- 🧪 **Statements**: ${obj.statements.pct}% | **Branches**: ${obj.branches.pct}% | **Functions**: ${obj.functions.pct}% | **Lines**: ${obj.lines.pct}%`;

              const body = `### ✅ Jest Code Coverage Report\n${format(summary)}\n\n📁 [Download HTML Coverage Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) → Artifacts section`;

              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body
              });
            }

      - name: Send Metrics to ServiceNow
        run: |
          echo "📡 Sending metrics to ServiceNow..."

          PR_NUMBER="${{ github.event.pull_request.number }}"
          PR_TITLE="${{ github.event.pull_request.title }}"
          PR_LINK="${{ github.event.pull_request.html_url }}"
          BRANCH_NAME="${{ github.head_ref }}"
          PR_AUTHOR="${{ github.event.pull_request.user.login }}"

          if [ "${{ github.event.pull_request.draft }}" = "false" ]; then
            MERGE_STATE="Ready"
          else
            MERGE_STATE="Draft"
          fi

          REPO="${{ github.repository }}"
          OWNER=$(echo "$REPO" | cut -d'/' -f1)
          REPO_NAME=$(echo "$REPO" | cut -d'/' -f2)

          FILES="${{ steps.detect_files.outputs.found_files }}"
          JS_FILE=""
          SPEC_FILE=""

          for FILE in $FILES; do
            RAW_URL="https://raw.githubusercontent.com/$OWNER/$REPO_NAME/refs/heads/$BRANCH_NAME/$FILE"
            SPEC_FILE="$RAW_URL"

            BASE_JS_FILE=$(basename "$FILE")
            JS_NAME="${BASE_JS_FILE/.spec.js/.js}"
            JS_FILE="https://raw.githubusercontent.com/$OWNER/$REPO_NAME/refs/heads/$BRANCH_NAME/src/$JS_NAME"
          done

          SUMMARY=$(cat coverage/coverage-summary.json | jq -c '.')

          PAYLOAD=$(cat <<EOF
{
  "jsfilePath": "$JS_FILE",
  "specJsfilePath": "$SPEC_FILE",
  "prNumber": "$PR_NUMBER",
  "prlink": "$PR_LINK",
  "Author": "$PR_AUTHOR",
  "MergeStatus": "$MERGE_STATE",
  "prName": "$PR_TITLE",
  "BranchName": "$BRANCH_NAME",
  "coverage": {
    "files": {
      "allfiles": {
        "lines": $(echo $SUMMARY | jq '.total.lines.pct'),
        "branch": $(echo $SUMMARY | jq '.total.branches.pct')
      },
      "mathUtil.js": $(echo $SUMMARY | jq '.["mathUtil.js"]')
    }
  }
}
EOF
)

          echo "$PAYLOAD"

          curl -X POST "https://hackai.service-now.com/api/snc/nowunitmetricsapi" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD"
