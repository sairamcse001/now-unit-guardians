const funcs = require('../src/mathUtil1'); // Adjust path if needed

test('Add test', () => {
  funcs.add(2, 3); // no expect
});


test('Sub test', () => {
  funcs.subtract(9, 4); // no assertion
});

// __tests__/mathUtil2.spec.js

test('placeholder', () => {
  expect(true).toBe(true);
});

